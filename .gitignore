venv/
*.db
*.db-wal
*.db-shm
*.db-journal
*.db.backup.*
*.db.pre_reset_*
blotter_replica.db.*
__pycache__/
*.pyc
uploads/
records/
*.log
config.py
.env
.env.local
.env.bak.*
.env.save
.secrets/
.claude/
db_backups/
backup.log
.sisyphus/
.hermes/
crime-extractor/
reports/pattern_conversion_*.csv
# Civil filings: exports and local capture artifacts (generated).
reports/civil_filings/*.csv
reports/civil_filings/*.json
reports/civil_filings/*.env
vapid_private.pem

# Crash-recovery temp files (Python's NamedTemporaryFile + .tmp suffix).
scrapers/*.tmp.*
*.tmp.*.py

# Agent queue: generated outputs from openclaw/newsroom agents, not source.
agent-queue/

# Daily chart snapshots regenerated by the scheduler — not source.
static/charts/*.png
